params {
    modules_testdata_base_path = 'https://raw.githubusercontent.com/nf-core/test-datasets/modules/data/'
}

process {

    // Two PROKKA tasks, one per contig; small enough that a CI runner runs both.
    withName: 'PROKKA' {
        cpus   = 1
        memory = 2.GB
    }

}
