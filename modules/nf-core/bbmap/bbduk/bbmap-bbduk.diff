Changes in component 'nf-core/bbmap/bbduk'
'modules/nf-core/bbmap/bbduk/environment.yml' is unchanged
Changes in 'bbmap/bbduk/meta.yml':
--- modules/nf-core/bbmap/bbduk/meta.yml
+++ modules/nf-core/bbmap/bbduk/meta.yml
@@ -44,6 +44,20 @@
           pattern: "*fastq.gz"
           ontologies:
             - edam: http://edamontology.org/format_3989 # GZIP
+  reads_removed:
+    - - meta:
+          type: map
+          description: |
+            Groovy Map containing sample information
+            e.g. [ id:'test', single_end:false ]
+      - "*.matched.fq.gz":
+          type: file
+          description: |
+            Reads that matched the contaminants reference (kmer filtering) and were removed from
+            the main output; only produced when a contaminants reference is provided
+          pattern: "*.matched.fq.gz"
+          ontologies:
+            - edam: http://edamontology.org/format_3989 # GZIP
   log:
     - - meta:
           type: map

Changes in 'bbmap/bbduk/main.nf':
--- modules/nf-core/bbmap/bbduk/main.nf
+++ modules/nf-core/bbmap/bbduk/main.nf
@@ -12,7 +12,8 @@
     path contaminants
 
     output:
-    tuple val(meta), path('*.fastq.gz'), emit: reads
+    tuple val(meta), path('*.fastq.gz')   , emit: reads
+    tuple val(meta), path('*.matched.fq.gz'), emit: reads_removed, optional: true
     tuple val(meta), path('*.log')     , emit: log
     tuple val("${task.process}"), val('bbmap'), eval('bbversion.sh | grep -v "Duplicate cpuset"'), emit: versions_bbmap, topic: versions
 
@@ -25,6 +26,7 @@
     def raw      = meta.single_end ? "in=${reads[0]}" : "in1=${reads[0]} in2=${reads[1]}"
     def trimmed  = meta.single_end ? "out=${prefix}.fastq.gz" : "out1=${prefix}_1.fastq.gz out2=${prefix}_2.fastq.gz"
     def contaminants_fa = contaminants ? "ref=$contaminants" : ''
+    def matched  = contaminants ? (meta.single_end ? "outm=${prefix}.matched.fq.gz" : "outm1=${prefix}_1.matched.fq.gz outm2=${prefix}_2.matched.fq.gz") : ''
     """
     bbduk.sh \\
         -Xmx${task.memory.toGiga()}g \\
@@ -33,14 +35,17 @@
         threads=$task.cpus \\
         $args \\
         $contaminants_fa \\
+        $matched \\
         &> ${prefix}.bbduk.log
     """
 
     stub:
     def prefix = task.ext.prefix ?: "${meta.id}"
     def output_command  = meta.single_end ? "echo '' | gzip > ${prefix}.fastq.gz" : "echo '' | gzip > ${prefix}_1.fastq.gz ; echo '' | gzip > ${prefix}_2.fastq.gz"
+    def matched_command = contaminants ? (meta.single_end ? "echo '' | gzip > ${prefix}.matched.fq.gz" : "echo '' | gzip > ${prefix}_1.matched.fq.gz ; echo '' | gzip > ${prefix}_2.matched.fq.gz") : ''
     """
     touch ${prefix}.bbduk.log
     $output_command
+    $matched_command
     """
 }

'modules/nf-core/bbmap/bbduk/tests/main.nf.test.snap' is unchanged
'modules/nf-core/bbmap/bbduk/tests/nextflow.config' is unchanged
Changes in 'bbmap/bbduk/tests/main.nf.test':
--- modules/nf-core/bbmap/bbduk/tests/main.nf.test
+++ modules/nf-core/bbmap/bbduk/tests/main.nf.test
@@ -82,7 +82,8 @@
                 { assert process.success },
                 { assert process.out.reads.get(0).get(1).endsWith("test.trim.fastq.gz") },
                 { assert path(process.out.log.get(0).get(1)).getText().contains("Input is being processed as unpaired")},
-                { assert snapshot(process.out.findAll { key, val -> key.startsWith('versions') }).match() }
+                { assert snapshot(process.out.reads_removed,
+                                  process.out.findAll { key, val -> key.startsWith('versions') }).match() }
 
             )
         }
@@ -111,7 +112,8 @@
                 { assert process.out.reads.get(0).get(1).get(0).endsWith("test.trim_1.fastq.gz") },
                 { assert process.out.reads.get(0).get(1).get(1).endsWith("test.trim_2.fastq.gz") },
                 { assert path(process.out.log.get(0).get(1)).getText().contains("Input is being processed as paired")},
-                { assert snapshot(process.out.findAll { key, val -> key.startsWith('versions') }).match() }
+                { assert snapshot(process.out.reads_removed,
+                                  process.out.findAll { key, val -> key.startsWith('versions') }).match() }
 
             )
         }

************************************************************
