Changes in component 'nf-core/bbmap/align'
'modules/nf-core/bbmap/align/environment.yml' is unchanged
'modules/nf-core/bbmap/align/meta.yml' is unchanged
Changes in 'bbmap/align/main.nf':
--- modules/nf-core/bbmap/align/main.nf
+++ modules/nf-core/bbmap/align/main.nf
@@ -38,6 +38,13 @@
         db = "ref=${ref}"
     }
 
+    memory = '-Xmx3g'
+    if ( ! task.memory ) {
+        log.info '[BBMap Align]: Available memory not known, defaulting to 3 GB. Specify process memory requirements to change this.'
+    } else {
+        memory = "-Xmx${Math.round(Math.max(1, Math.floor(task.memory.toGiga() * 0.9)))}g"
+    }
+
     """
     bbmap.sh \\
         $db \\
@@ -45,7 +52,7 @@
         out=${prefix}.bam \\
         $args \\
         threads=$task.cpus \\
-        -Xmx${task.memory.toGiga()}g \\
+        $memory \\
         &> ${prefix}.bbmap.log
     """
     stub:

'modules/nf-core/bbmap/align/tests/main.nf.test.snap' is unchanged
'modules/nf-core/bbmap/align/tests/nextflow.config' is unchanged
'modules/nf-core/bbmap/align/tests/main.nf.test' is unchanged
************************************************************
